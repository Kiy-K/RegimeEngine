# Legacy fallback for pip users.
# Preferred workflow: use uv with pyproject.toml + uv.lock
#
#   uv sync --extra train --extra dev --extra viz --extra gui
#
# If you still use pip:
#   pip install -e ".[train,dev,viz,gui]"

-e ".[train,dev,viz,gui]"
